# try : code which might throw an exception
          #exceptiontype : value error
                        #type error
                        # zero division error
#except : code to handle the exception

#finally : code which will be executed regardless of exception